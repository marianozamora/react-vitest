// import { describe, test, expect } from 'vitest';
import Accordion from './index';
import { render,screen } from '@testing-library/react';

describe('Accordion', () => {
    test('should add two numbers', () => {
        render(<Accordion title="test">test</Accordion>);
        expect()
    })
});